# Auxiliares de LaTeX (se generan al compilar y no se versionan)
*.aux
*.log
*.out
*.toc

# Generados (se regeneran con `make`; no se versionan para ahorrar espacio)
*.pdf
*.svg

# El membrete es un asset de diseño (no un generado): se versiona aunque *.pdf lo ignore
!.opencode/skills/boletin-latex-pdf/assets/membrete.pdf

# Sistema
.DS_Store
